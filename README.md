# fajia.online starter

这是 `fajia.online` 的总入口最小可部署版本。

## 当前路由
- `/` 总入口
- `/timeline/` 时间线占位页
- `/monitor/` 舆情观察占位页
- 直播游戏屋跳转 `https://fajia-livegame.top`

## EdgeOne Pages
这是纯 HTML/CSS 静态站，不需要 npm。

建议：
- 根目录：仓库根目录 `.`
- 构建命令：留空
- 输出目录：`.`（若界面允许直接静态部署，也可留空）
- 生产分支：`main`
